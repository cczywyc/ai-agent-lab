"""
搜索 Agent 配置中心
集中管理：API 客户端、模型选择、System Prompt、Agent Loop 常量

v2.0 — 第三周升级版
- 新版 System Prompt（默认搜索，例外直答）
- 新增 Agent Loop 控制常量
- 新增 URL 黑名单
"""

import os

from dotenv import load_dotenv
from openai import OpenAI

# ============================================================
# API 客户端配置
# ============================================================
load_dotenv()
client = OpenAI(
    api_key=os.getenv("DASHSCOPE_API_KEY"),
    base_url=os.getenv("DASHSCOPE_BASE_URL", "https://dashscope.aliyuncs.com/compatible-mode/v1")
)
MODEL = "qwen-plus"

# ============================================================
# System Prompt — v2.0
# ============================================================
# 设计原则：
#   1. 默认搜索，例外直答（翻转默认方向）
#   2. 用 few-shot examples 代替规则堆砌
#   3. 包含降级许可（配合 Agent Loop 的降级注入）
#   4. 控制在 ~500 字，避免过长被模型"稀释"

SYSTEM_PROMPT = """
# Role
你是一个联网搜索助手。你通过搜索引擎获取最新、准确的信息来回答用户问题。
你有两个工具：web_search（搜索）和 fetch_webpage（读取网页正文）。

# Core Rule: Search First
对于以下类型的问题，你 **必须** 先使用 web_search，禁止直接从记忆回答：
- 涉及具体人物、事件、时间、数据的事实性问题
- 涉及技术产品、框架、协议的特性、版本、对比
- 你不确定答案是否准确或可能过时的任何问题
- 你不认识或不了解的任何名词、术语、缩写（即使看起来像无意义字符串，也应先搜索确认）

仅以下情况允许不搜索直接回答：
- 纯创意写作（写诗、写故事、起名字）
- 数学计算或逻辑推理
- 日常闲聊和打招呼
- 用户明确要求你不要搜索

当你不确定该不该搜索时，选择搜索。宁可多搜一次，也不要给出未经验证的回答。

# Tool Usage Guidelines
1. web_search：传入简洁的英文关键词，通常 2-5 个词效果最好
2. fetch_webpage：仅在搜索摘要信息不够详细时使用，优先用搜索摘要回答
3. 如果 fetch_webpage 连续失败（如 403），不要反复重试同一类网站，改用已有的搜索摘要来回答

# Output Requirements
- 使用与用户相同的语言回答
- 如果基于搜索结果回答，在末尾简要注明信息来源
- 如果信息不完整（如只有搜索摘要没有完整文章），主动告知用户
- 回答要简洁有条理，避免重复搜索结果中的原文

# Examples
用户：2024年诺贝尔物理学奖颁给了谁？
正确行为：调用 web_search("2024 Nobel Prize Physics winner")，基于结果回答。
错误行为：直接从记忆回答（信息可能不准确或过时）。

用户：什么是 MCP 协议？
正确行为：调用 web_search("MCP Model Context Protocol")，基于结果解释。
错误行为：直接回答（技术概念有最新发展，需要搜索验证）。

用户：写一首关于秋天的诗
正确行为：直接创作，不需要搜索。
"""

# ============================================================
# Agent Loop 控制常量
# ============================================================

# 最大循环轮次（从 v1 的 5 提升到 6，给降级回答留空间）
MAX_TURNS = 6

# 连续工具失败次数达到此阈值后触发降级
MAX_CONSECUTIVE_ERRORS = 2

# 上下文裁剪阈值（字符数，粗略≈token 数）
# 第三周简化版：只做警告，不实际裁剪
MAX_CONTEXT_CHARS = 6000

# ============================================================
# 纠正指令（当模型应该搜索但直接回答时注入）
# ============================================================

CORRECTION_MESSAGE = (
    "请不要直接回答这个问题。先使用 web_search 工具搜索相关信息，"
    "然后基于搜索结果给出有来源依据的回答。"
)

# ============================================================
# 降级指令（当工具连续失败时注入）
# ============================================================

FALLBACK_MESSAGE = (
    "[System Notice] 多次获取网页内容失败。"
    "请直接基于你已经获得的搜索摘要（snippets）来回答用户的问题。"
    "在回答开头注明：'以下回答基于搜索摘要，未能获取完整文章内容。'"
    "不要再尝试使用 fetch_webpage 工具。"
)

# ============================================================
# URL 黑名单（已知反爬站点，fetch_webpage 直接拒绝）
# ============================================================

BLOCKED_DOMAINS = [
    "medium.com",
    "towardsdatascience.com",
    "datacamp.com",
    "linkedin.com",
    "quora.com",
    "slideshare.net",
    "zhihu.com",
]
